一开始的思路：
我对于这道题的思路是建立一个字典,key是含有单词字母的集合，value是有这些集合的index，对所有坐标进行一次遍历最后输出，这样有两个问题，一个是我没法控制输出结果的顺序，另一个是好像集合没有办法作为key。
普通 set 有两个问题：
set 可修改，因此不能作为字典的 key。
更重要的是，它会丢失字母出现次数。

学会使用tuple。
这里用tuple(sorted(words))作为字母异味词的key，用他们的坐标作为value，构成一个哈希表
  
