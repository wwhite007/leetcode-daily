这个题目好难，难在我没还有办法确定num[0:i]与num[0:i+1]的关系

从左往右，依次计算“以每个数字作为结尾”时，最长递增子序列有多长。

关键不是直接寻找整个数组的答案，而是先解决很多个小问题。

solution1是标准做法，注意这个max，筛选最长递增子序列长度。
solution2可以用二分查找来优化。这里tail[i]储存的是长度是i的时候对应的tail的最小值
这样子这个tail一定是升序排列，遇到一个新的数字输入的时候可以用二分查找找位置，然后找到以后直接替换最小的大于它的数字即可。如果在最后一位直接加上去即可。
