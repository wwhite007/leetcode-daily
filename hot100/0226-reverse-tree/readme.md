# 翻转二叉树，左右翻转。
## 关于思路
这里是要对于tree进行操作而不是输出列表。
所以是对于node的结构进行内部调整而不是外部输出。

## 关于语法
class 是一个对象的模板，它里面可以包含变量和函数。
self 的作用：告诉函数：你现在操作的是哪个对象。
invertTree 是属于 Solution 这个对象的方法。
所以调用方法需要：对象.方法()
self 表示“属于对象自己的东西”，普通变量表示“函数传进来的临时东西”。分别对应“我的”和“别人传给我的”
